<!DOCTYPE html>

<html>
  <head>
    <meta charset="utf-8" />
    <link href="/css/category.css" rel="stylesheet"/>
    <title><%= title %></title>
    <%- include('../partials/head') %>
    <style>
       
    </style>
  </head>

  <body>
    <%- include('../partials/nav') %>
    <br>
    <div class="main">
        <div class="left">
            <h3>CATEGORY</h3>
            <ul class="bar">
                <li><a href="/category1">나이대</a></li>
                <li><a href="/category2">성별</a></li>
                <li><a href="/category3">태그</a></li>
            </ul>
        </div>

        <div class="center">
            <div class="tab">
                <ul class="list">
                    <% for (var j=0; j<tag.length; j++) { %>
                        <li class="is_on">
                            <a href="#tab<%=j%>" class="btn"><%=tag[j].tag_cont%></a>
                            <div id="tab<%=j%>" class="cont">
                                <% for (var i=0; i<row[j].length; i++) { %>
                                    <a href="/Otherboard/<%=row[j][i].post_num%>"><img src="<%=row[j][i].place_photo%>"/></a>
                                <% } %>
                            </div>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </div>

    <script>
        const tabList = document.querySelectorAll('.tab .list li');
        
        for(var a=0; a<tabList.length; a++) {
            tabList[a].classList.remove('is_on');
        }
        tabList[0].classList.add('is_on');

        for(var i=0; i<tabList.length; i++) {
            tabList[i].querySelector('.btn').addEventListener('click', function(e) {
                e.preventDefault();
                for(var j=0; j<tabList.length; j++) {
                    tabList[j].classList.remove('is_on');
                }
                this.parentNode.classList.add('is_on');
            });
        }
    </script>
  </body>
</html>



